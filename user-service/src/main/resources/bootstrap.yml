spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: userservice
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: 'refresh'