spring:
  cloud:
    config:
      uri: http://config-srv:8888 # without compose, use localhost
  application:
    name: servermanager
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: 'refresh'
