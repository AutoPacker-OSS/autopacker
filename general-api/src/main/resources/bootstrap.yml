spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: general
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: 'refresh'